import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    textInput: {
        borderColor: 'black',
        borderWidth: 2,
        borderRadius: 10,
        width: '100%',
        padding: 10,
        marginVertical: 10
      },
    label: {
        fontWeight: 'bold'
    }
});