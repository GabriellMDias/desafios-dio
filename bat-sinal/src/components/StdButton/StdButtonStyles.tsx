import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    button: {
        backgroundColor: 'gray',
        padding: 10,
        fontWeight: 'bold',
        fontSize: 20,
        color: 'white',
        borderRadius: 10,
        textAlign: 'center'
      }
});